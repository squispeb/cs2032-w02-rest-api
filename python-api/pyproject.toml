[project]
name = "python-api"
version = "0.1.0"
description = "Basic REST API with FastAPI and SQLite"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115.0",
  "uvicorn[standard]>=0.30.0",
]

[project.optional-dependencies]
dev = []

[tool.uv]
required-version = ">=0.9.0"
